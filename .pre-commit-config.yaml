repos:
  - repo: https://github.com/pre-commit/mirrors-clang-format
    rev: v17.0.6
    hooks:
      - id: clang-format
        name: clang-format (C++)
        files: \.(cpp|hpp|cc|h)$
        args: [--style=file] # uses your .clang-format
        stages: [commit]
